# The ACTUAL runtime dependencies of scripts/ and models/, derived by
# grepping every import/from statement in this repo (2026-08-16) rather
# than trusting requirements-full-freeze.txt, which is a raw `pip freeze`
# of a broader dev environment and pulls in ~150 packages the pipeline
# never imports (JupyterLab, GitPython, sentry-sdk, prometheus_client,
# fredapi, pycoingecko, ...). Prefer this file for a clean pipeline
# install; use requirements-full-freeze.txt only if you specifically want
# the notebook/dev tooling that came with the original environment too.
#
# NOTE: repo-root requirements.txt is a *different* file — it's the
# dashboard's minimal dependency list (streamlit, pandas, plotly), the one
# Streamlit Community Cloud actually deploys from. Don't confuse the two;
# this file is for running the model pipeline locally, not the dashboard.
#
# torch's CPU-only wheels (+cpu suffix) aren't on PyPI's default index —
# this line points pip at PyTorch's own index so `torch` resolves.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy
pandas
scipy
pyyaml
torch
torch-geometric
statsmodels
arch
scikit-learn
matplotlib
yfinance

# Used only if present — both are wrapped in try/except in the code, so
# the pipeline runs fine without them:
networkx          # scripts/generate_figures.py fig6 (contagion network plot)
wandb             # models/train.py optional experiment tracking
